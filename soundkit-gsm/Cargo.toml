[package]
name = "soundkit-gsm"
version = "0.12.3"
edition = "2021"
license = "MIT"
description = "GSM 06.10 codec wrapper for soundkit using libgsm."
repository = "https://github.com/wavey-ai/soundkit"

[dependencies]
gsm-sys = "0.0.2"
soundkit = { path = "../soundkit" }
